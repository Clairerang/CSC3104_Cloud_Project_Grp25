{
  "name": "backend",
  "version": "1.0.0",
  "description": "npm init -y\r npm install express mongoose kafkajs redis dotenv\r npm install nodemailer\r npm install winston cors",
  "main": "testProducer.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "kafkajs": "^2.2.4"
  }
}
